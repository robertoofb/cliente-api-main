import React from 'react';
import Navigation from './Navigation';

function Header(){
    return (
    <header class="barra">
        <div class="contenedor">
            <h1>Tamales</h1>
        </div>
    </header>
    )
}

export default Header;
